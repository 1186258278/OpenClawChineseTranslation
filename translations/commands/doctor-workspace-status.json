{
  "file": "src/commands/doctor-workspace-status.ts",
  "description": "工作区状态诊断",
  "replacements": {
    "`Eligible: ${skillsReport.skills.filter((s) => s.eligible).length}`": "`可用: ${skillsReport.skills.filter((s) => s.eligible).length}`",
    "`Missing requirements: ${": "`缺少依赖: ${",
    "`Blocked by allowlist: ${skillsReport.skills.filter((s) => s.blockedByAllowlist).length}`": "`被白名单阻止: ${skillsReport.skills.filter((s) => s.blockedByAllowlist).length}`",
    "\"Skills status\"": "\"技能状态\"",
    "`Fix: ${updateCommands[0]} && ${formatCliCommand(\"openclaw gateway restart\")}.`": "`修复：${updateCommands[0]} && ${formatCliCommand(\"openclaw gateway restart\")}。`",
    "\"Fix each drifted plugin:\"": "\"逐个修复漂移插件：\"",
    "`Then run ${formatCliCommand(\"openclaw gateway restart\")}.`": "`然后运行 ${formatCliCommand(\"openclaw gateway restart\")}。`",
    "note(lines.join(\"\\n\"), \"Plugin version drift\")": "note(lines.join(\"\\n\"), \"插件版本漂移\")",
    "`Loaded: ${loaded.length}`": "`已加载: ${loaded.length}`",
    "`Imported: ${imported.length}`": "`已导入: ${imported.length}`",
    "`Disabled: ${disabled.length}`": "`已禁用: ${disabled.length}`",
    "`Errors: ${errored.length}`": "`错误: ${errored.length}`",
    "note(lines.join(\"\\n\"), \"Plugins\")": "note(lines.join(\"\\n\"), \"插件\")",
    "note(lines.join(\"\\n\"), \"Plugin diagnostics\")": "note(lines.join(\"\\n\"), \"插件诊断\")"
  }
}
